# --- core (CPU-side, pinned for reproducibility) ---------------------------
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
tabulate==0.9.0            # pandas.to_markdown backend

# --- ML / optimization -----------------------------------------------------
# torch: install the build that matches YOUR CUDA. The line below is a CUDA 12.x
# wheel; see https://pytorch.org/get-started/locally/ for the exact command.
torch==2.3.1

cvxpy==1.5.2              # CPU baseline QP solver for the optimizer

# --- dashboard -------------------------------------------------------------
dash==2.17.1
dash-bootstrap-components==1.6.0
plotly==5.22.0

# --- GPU libraries: install per your CUDA version, NOT pinned here ---------
# These are environment-specific (CUDA major version, GPU compute capability).
# Verify against docs before installing:
#   cupy-cuda12x            # RawKernel backtester + cupy baselines (CUDA 12.x)
#   nvidia-cuopt            # cuOpt QP solver (check the install matrix for your GPU)
#
# Example (adjust to your setup):
#   pip install cupy-cuda12x
#   pip install nvidia-cuopt        # or follow the cuOpt install guide

# --- dev (optional) --------------------------------------------------------
ruff==0.5.0
pytest==8.2.2             # CPU-only test suite (make test); no GPU required
